[project]
name = "brain-post-makeup-artist"
version = "0.1.0"
description = "Codex Skill for deterministic BRAIN forum composition and guarded publishing"
requires-python = ">=3.11"
dependencies = [
  "beautifulsoup4>=4.12",
  "mistune>=3.0",
  "playwright>=1.50",
  "requests>=2.32",
]

[tool.uv]
package = false
